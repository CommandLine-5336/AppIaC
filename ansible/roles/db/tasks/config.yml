---
- name: Ensure bind-address is commented out in system config
  ansible.builtin.lineinfile:
    path: /etc/mysql/mariadb.conf.d/50-server.cnf
    regexp: '^bind-address\s*='
    line: 'bind-address = 0.0.0.0'
    state: present
  notify: Restart mariadb
