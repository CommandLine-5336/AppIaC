consul {
  address = "127.0.0.1:8500"
}

template {
  source      = "/etc/consul-template.d/nginx.conf.ctmpl"
  destination = "/etc/nginx/sites-available/birdswatching.site.conf"
  command     = "systemctl reload nginx"
}