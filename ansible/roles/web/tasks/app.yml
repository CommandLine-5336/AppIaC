- name: Clone App files
  ansible.builtin.copy:
    src: /var/lib/jenkins/workspace/multi_repo/BirdWathcingApp
    dest: "{{ web_app_dir }}"
    owner: "{{ web_user }}"
    group: "{{ web_user }}"
    mode: '0644'
